@model Role
@{
    ViewData["Title"] = "Create Role";
    ViewData["ActivePage"] = "Roles";
}

<div style="padding: 20px; max-width: 900px; margin: 0 auto;">
    <div class="topbar">
        <h1 class="page-title">Create New Role</h1>
    </div>

    <form asp-action="Create" method="post">
        @Html.AntiForgeryToken()

        <div class="card">
            <div class="card-header">
                <h3 style="margin: 0; font-family: 'Syne', sans-serif; font-size: 16px; font-weight: 700;">Role Details</h3>
            </div>
            <div class="form-body">
                <div class="field">
                    <label>Role Name *</label>
                    <input type="text" name="Name" placeholder="e.g., Editor, Viewer" autofocus required />
                </div>
            </div>
        </div>

        <div class="card" style="margin-top: 24px;">
            <div class="card-header">
                <h3 style="margin: 0; font-family: 'Syne', sans-serif; font-size: 16px; font-weight: 700;">Permissions</h3>
            </div>
            <div class="form-body">
                @{
                    var accessFlags = Enum.GetValues<PageAccess>()
                        // .Where(a => a != PageAccess.FullAccess)
                        .ToList();
                }

                @foreach (var page in Enum.GetValues<Pages>())
                {
                    <div style="background: var(--bg); border: 1px solid var(--border); border-radius: 8px; padding: 16px; margin-bottom: 12px;">
                        <div style="font-weight: 500; margin-bottom: 12px;">@(page)</div>
                        <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(150px, 1fr)); gap: 12px; margin-left: 12px;">
                            @foreach (var access in accessFlags)
                            {
                                <label style="display: flex; align-items: center; gap: 8px; cursor: pointer; font-size: 13px;">
                                    <input type="checkbox" 
                                   name="perms_@((int)page)_@((int)access)" 
                                   value="1" />
                                    @access
                                </label>
                            }
                        </div>
                    </div>
                }
            </div>
        </div>

        <div class="card-footer" style="display: flex; gap: 10px; justify-content: flex-end; margin-top: 24px;">
            <a asp-action="Index" class="btn-cancel">Cancel</a>
            @if (User.HasAccess(Pages.Roles, PageAccess.Create)){
                <button type="submit" class="btn-save">Create Role</button>
            }
        </div>
    </form>
</div>